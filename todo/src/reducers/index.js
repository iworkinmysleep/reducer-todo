export const initialTodoState = {
	item: "Learn about reducers",
	completed: false,
	id: Date.now(),
};

export const addTodoReducer = (state, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
